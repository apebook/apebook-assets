KISSY.add('apebook/bind-github/go-github',["node"],function(S ,require, exports, module) {
 var $ = require('node').all;

module.exports = function() {
  var $tip = $('.J_GithubTip');
  if(!$tip.length){
    return false;
  }
  //location.href = $tip.attr('data-url');
};
});